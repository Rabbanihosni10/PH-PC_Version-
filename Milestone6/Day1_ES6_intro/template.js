// for template string or dynamic string we can use `` and ${}
 const first='Jaan';
const last='pakhi';
const greet='potash potash'
const name=first+" "+last+" "+greet;
console.log(name); 
const a =10;
const b =20;
const result='The sum of'+a+ 'and '+b+'is'+(a+b);
const loki={name: 'Tobarok', degignation: 'tomarok'}
//back tick4
const math=`The sum of ${a} and ${b} is ${a+b} and ${loki.name}`;
console.log(math);

const email='Hi John Sina\n'+
'john sina re sina sina lage\n'+
'tumi maramari korba naki';
console.log(email);
const challenge=`John cena
cena naki ochena
mair dimu procur
`
console.log(challenge);